import { create, NH1, NH2, NCard, NMessageProvider, NButton, NForm, NFormItemRow, NInput } from 'naive-ui'

export default create({
	components: [NH1, NH2, NCard, NMessageProvider, NButton, NForm, NFormItemRow, NInput],
})
